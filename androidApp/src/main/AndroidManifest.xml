<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        NOTE: READ_CONTACTS is intentionally NOT declared. Contact import uses
        the Android system contact picker (ACTION_PICK via PickContact), which
        returns exactly one contact and requires no permission — compliant with
        Google's Contact Picker policy (see supabase/../COMPLIANCE_TODO item 16).
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Strip biometric permissions injected transitively by
         androidx.credentials <- Google Identity (via Supabase Google Sign-In).
         No app code uses biometrics — see COMPLIANCE_TODO item 6. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" tools:node="remove" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" tools:node="remove" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foster.Splash">
        <activity
            android:exported="true"
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="auth-callback"
                    android:scheme="app.usefoster" />
            </intent-filter>
        </activity>

        <!-- Not exported: no other app may trigger check-in reminders.
             (Was exported=true for manual QA broadcasts — reverted 2026-09-08,
             COMPLIANCE_TODO item 1.) -->
        <receiver
            android:exported="false"
            android:name="app.usefoster.shared.notifications.CheckInReminderReceiver" />

        <receiver
            android:exported="false"
            android:name="app.usefoster.shared.notifications.ReminderBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
